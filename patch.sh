sudo apt-get --assume-yes update        # Fetches the list of available updates
sudo apt-get --assume-yes upgrade       # Strictly upgrades the current packages
sudo apt-get --assume-yes dist-upgrade  # Installs updates (new ones)

